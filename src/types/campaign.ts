export type ICampaignType = {
  _id: string;
  campaignName: string;
  linkedAppId: string;
  description: string;
  campaignId: string;
};

export type ICampaignFilters = {
  id: string;
  campaignName: string;
};
