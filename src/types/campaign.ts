export type ICampaignType = {
  _id: string;
  campaignName: string;
  linkedBot: string;
  description: string;
  campaignId: string;
};

export type ICampaignFilters = {
  id: string;
  campaignName: string;
};
